<p align='center' >KTH LARGEST ELEMENT IN AN ARRAY</p>

## WAYS TO SOLVE

1. COUNTING SORT
    - SPACE
        * O(MAX(N))
    - TIME
        * O(N * R)
2. MIN HEAP
    - SPACE
        * O(K)
    - TIME
        * O(NLOGK)
3. QUICK SELECT
    - SPACE
        * O(1)
    - TIME
        * AVG = O(N)
        * WORST = O(N*N)